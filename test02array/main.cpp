#include <iostream>
using namespace std;

int main() {
    int arr[5] = {10, 20, 30, 40, 50}; // declare and initialize an array of integers
    for (int i = 0; i < 5; i++) { // loop through the array
        cout << "Element " << i << " of the array is: " << arr[i] << endl;
    }
    return 0;
}
